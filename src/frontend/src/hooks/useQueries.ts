import { useQuery } from '@tanstack/react-query';
import { useActor } from './useActor';

// Placeholder file - customer query functionality has been removed
// This file is kept to avoid breaking imports but contains no active queries
